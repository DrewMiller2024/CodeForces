#include <bits/stdc++.h>
 
using namespace std;
typedef long long ll;
typedef vector<ll> vll;
typedef pair<ll, ll> pll;

int32_t main() {
	cin.tie(NULL); cout.tie(NULL);
  ll q;
  cin >> q;
  for (ll i = 0; i < q; i++) {
    ll l, r, m;
    cin >> l >> r >> m;
    
  }
  return 0;
}